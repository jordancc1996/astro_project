---
---

<section class="relative h-screen min-h-[700px] flex items-center justify-center overflow-hidden">
  <!-- Background Image - z-index: 0 -->
  <div class="absolute inset-0 z-0">
    <img
      src="https://images.unsplash.com/photo-1589909202802-8f4aadce1849?w=1920&q=80"
      alt="Buenos Aires at sunset"
      class="w-full h-full object-cover"
      style="filter: saturate(0.9);"
    />
  </div>

  <!-- Dark Gradient Overlay - z-index: 1 -->
  <div class="absolute inset-0 z-[1] hero-overlay-dark"></div>

  <!-- Content - z-index: 10 -->
  <div class="relative z-10 container-luxury text-center max-w-4xl mx-auto px-6">
    <!-- Small Label -->
    <p class="hero-label opacity-0 animate-fade-up delay-100">
      Exclusive Residency & Citizenship Services
    </p>

    <!-- Main Heading -->
    <h1 class="hero-heading opacity-0 animate-fade-up delay-200">
      Your Gateway
      <br />
      <span class="italic">to Argentina</span>
    </h1>

    <!-- Subheading -->
    <p class="hero-description opacity-0 animate-fade-up delay-300">
      Expert guidance through investment residency, retirement visas, 
      and a streamlined path to citizenship.
    </p>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mt-10 opacity-0 animate-fade-up delay-400">
      <a href="/visa/investor" class="hero-btn-glass">
        Explore Visas
      </a>
      <a href="/contact" class="hero-btn-solid">
        Book Consultation
      </a>
    </div>
  </div>

  <!-- Scroll Indicator - z-index: 10 -->
  <button 
    id="scroll-down" 
    class="absolute bottom-10 left-1/2 -translate-x-1/2 z-10 flex flex-col items-center gap-3 text-white/70 hover:text-white transition-colors duration-300 cursor-pointer opacity-0 animate-fade-in delay-600"
  >
    <span class="font-sans text-[10px] font-normal tracking-luxury uppercase text-shadow-hero">
      Scroll
    </span>
    <svg class="w-5 h-5 animate-bounce-gentle" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
      <path stroke-linecap="round" stroke-linejoin="round" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </button>
</section>

<style>
  /* Dark overlay for better text contrast */
  .hero-overlay-dark {
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.4) 0%,
      rgba(0, 0, 0, 0.5) 50%,
      rgba(0, 0, 0, 0.6) 100%
    );
  }

  /* Text shadow for better readability */
  .text-shadow-hero {
    text-shadow: 0 2px 12px rgba(0, 0, 0, 0.4);
  }

  /* Hero Label Styling */
  .hero-label {
    font-family: 'Montserrat', system-ui, sans-serif;
    font-size: 11px;
    font-weight: 400;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #FFFFFF;
    margin-bottom: 1.5rem;
    text-shadow: 0 2px 12px rgba(0, 0, 0, 0.4);
  }

  /* Hero Heading Styling */
  .hero-heading {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 300;
    line-height: 1.1;
    letter-spacing: 0.02em;
    color: #FFFFFF;
    margin-bottom: 1.5rem;
    text-shadow: 0 2px 16px rgba(0, 0, 0, 0.5);
  }

  /* Hero Description Styling */
  .hero-description {
    font-family: 'Montserrat', system-ui, sans-serif;
    font-size: 1rem;
    font-weight: 400;
    line-height: 1.7;
    color: #FFFFFF;
    opacity: 0.95;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    text-shadow: 0 2px 12px rgba(0, 0, 0, 0.4);
  }

  @media (min-width: 768px) {
    .hero-description {
      font-size: 1.125rem;
    }
  }

  /* Glass Button - Translucent with blur */
  .hero-btn-glass {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 16px 40px;
    font-family: 'Montserrat', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    text-decoration: none;
    color: #FFFFFF;
    background: rgba(255, 255, 255, 0.15);
    border: 2px solid #FFFFFF;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .hero-btn-glass:hover {
    background: rgba(255, 255, 255, 0.25);
    transform: translateY(-2px);
  }

  /* Solid White Button */
  .hero-btn-solid {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 16px 40px;
    font-family: 'Montserrat', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    text-decoration: none;
    color: #2C2C2C;
    background: #FFFFFF;
    border: 2px solid #FFFFFF;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .hero-btn-solid:hover {
    background: #F5F5F5;
    transform: scale(1.02);
  }
</style>

<script>
  document.getElementById('scroll-down')?.addEventListener('click', () => {
    window.scrollTo({
      top: window.innerHeight,
      behavior: 'smooth',
    });
  });
</script>
